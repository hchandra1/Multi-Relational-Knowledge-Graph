#include "Edge.h" // this is my edge.cpp file

Edge::Edge(double edgeWeight, const std::string &edgeLabel, const std::string &destId)
    : weight(edgeWeight), label(edgeLabel), destination(destId)
     {

     }
